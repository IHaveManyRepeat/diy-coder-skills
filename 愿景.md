# 我的终极目标是简化ai研发代码

## bmad工作流使用痛点
我在使用claude code编码时遇到了很多问题，使用bmad工作流时遇到了很多痛点，

### 安装阶段
1. 首先每到一个目录我需要安装一遍工作流，比如bmad工作流

### 审核阶段
1. bmad工作流有bmad—help，这是好的一点，可是prd、架构、ecpis、story、每次文档审核让我抓狂
2. 关于文档，相同的内容，不要变化说法，这样审核量大大增加，
3. 文档里的内容要用精简的语言写，而不是一堆废话。这应该是默认项，而不是我每次强调的内容。
4. 为了减少审核量，应该把prd里的内容和task直接放在一起，epics和story我实在不清楚他们的作用。应该想尽办法减少我需要审核的文档量。

### 工作流方面
1. 做一个自动化技能，直接就会自动执行prd创建、校验、架构创建、校验、epic、story、task划分，全文档校验，然后把审核好的文档加入到队列里等待执行，我随时决定是否执行。
2. bmad工作流里的东西不够自动化，比如prd生成后，我得自己想起来去调用对抗技能，不然就不会执行，其他文档都是一样。这类地方有很多，
3. 我希望做一个文档敲定好就能甩手让ai去完成的工作流，目前看，bmad里有，可是前面审核文档的时间太繁琐了，而且很多细节我都不了解到底是否合适，尤其是task。
4. 进入队列里的任务可以暂停，处于暂停状态时，当前任务完成后自动暂停。启动也可以随时在断点处启动。

### bug修复方面
1. 我遇到很多bug，bmad没有专门修复bug的工作流，不得已我得自己做一个，类似的，bmad工作流里有很多地方都并不完善
2. 修复bug的过程，我希望是先梳理根本原因，然后梳理怎么从源头杜绝这类bug的生成，最后生成一份报告，等用户审批，审批完成，ai把bug修复，然后要把杜绝一类bug的解决方案要想办法落地，现在没有这种机制。

### 通用模块如何复用
6. 还有一些功能模块是很通用的模块，比如登录，这类模块应该成为一种成熟的类似套件一样的东西，哪些是不能变的，哪些是可以变的，可以变的由用户发挥，不能变得其实就是做一个登录功能必须要考虑的东西，我第二次做登录功能时，我希望前面做的经验能复用，不管是设计方面要考虑的问题，还是测试方面的质量把关要求。
7. ai说测试过了，可是测试用例质量有问题，即便使用bmad工作流做测试用例质量也不够，变异测试成本高，但是不改代码，能不能提前根据产品原型或者逻辑直接生成可以经受变异测试考验的测试用例了，设计之初就处理好多好。
8. 配置pre-commit做代码提交钱的质量验证。设置ci，甚至于代码质量要求等等都应该变成默认项，而不是我不说，ai就不会做的东西。
9. 前端出现幽灵字段，ai说用openapi，可是用了之后，openapi文档首先难以审核，其次生成了ai还不按照文档去做，这本身就是要杜绝的问题，
10. 一些用户体验或者说标准问题，比如按钮不能重复点击的事，反复强调，应该有一个机制让我纠正过一次的问题，在下次编码中ai会自动避免。
11. bmad工作流无法在一个项目里连续开发多个版本
12. 我用bmad生成了设计稿，但是ai就是不按照设计稿去做前端


我希望补全剩余bmad工作流里的技能，然后

如何做skill质量检查，需要敲定办法

软件部署到不断网的服务器上，我现在的问题就是，
如何做模块化，登录这个模块，每个系统开发，那么其实有 一些共通的东西可以迁移，不共通的东西可以调整，因为不共通而影响其他共通部分的内容要考虑如何解决。

组分
1. 模型
  1. Api key
  2. 请求地址
  3. 模型
2. 工作单元
  1. 组成工作单元的工具
    1. Git
      1. Gh cli
    2. Claude
    3. codex
  2. 组成工作单元的步骤
  3. 组成工作单元的模型
  4. 工作单元的产出
3. 由工作单元组成的工作模块
4. 由工作模块组成的工作流
5. 工作目录
6. 工作agent
7. 记录
  1. 每一次聊天发给大模型的记录
  2. 每一次聊天，大模型发起了多少次请求
  3. 每一次聊天，大模型花费了多少token
  4. 每一次聊天，大模型的行动轨迹
  5. 每一个环节，行之有效的沉淀下来的经验或者工具或者流程
软件内容
1. 设置页
  1. 模型设置页：
    1. 可以添加修改删除各种供coding agent使用的模型，
    2. 为了让模型能适配不同的coding agent，那么请求地址要分agent，claude用支持claude的，codex用支持codex的
    3. 模型设置增加周限额和5小时限额以及月限额
  2. 工作单元设置页
    1. 可以添加修改删除未被使用的工作单元
    2. 对于正在被工作模块使用的工作单元可修改不可删除
    3. 对于正在被当前运行工作流使用的工作单元不可修改删除
    4. 工作单元可被各种工作模块复用
    5. 工作单元必须定义清楚自己的工作任务和工作产出
      1. 输入：执行该工作单元所必须的传入的内容
      2. 工作任务：该工作单元要执行的内容
      3. 使用攻击：为了完成工作任务而选择的工具，工具是内置的
      4. 工作产出：工作单元执行任务后的结果
  3. 工作模块
    1. 可以添加修改删除未被使用的工作模块
      1. 可以按照顺序排列工作单元，使得ai按照排序的工作单元做事
      2. 对于可以并行的工作单元要能使ai并行执行
    2. 对于目前正在工作流使用的工作模块可修改不可删除
    3. 对于正在被当前运行工作流使用的工作模块不可修改删除
    4. 工作模块可被各种工作流复用
  4. 工作流设置页
    1. 可以添加修改删除未被使用的工作李璐
      1. 可以按照顺序排列工作模块，使得ai按照排序的工作流做事
      2. 于可以并行的工作模块要能使ai并行执行
    2. 对于正在运行的工作流不可修改删除，其他的可修改删除
2. 工作台
  1. 顶部：设置入口
  2. 左侧：历史工作记录
  3. 右侧：单个工作记录的概览
    1. 简要和入口卡片
      1. token监控：用于分析钱花在哪里了，然后能有针对性的优化这些花钱的环节。
      2. 请求了多少次
      3. 写了多少行代码
      4. 工作情况概述：
        1. 目前工作流进行到哪一步了
        2. 各个工作模块工作单元的完成情况
      5. 用于查看工作流中涉及到的每个模型当前请求数是多少，还有多少到达限额
3. 功能
  1. 大模型额度预警功能
    1. 对于5小时限额，要到90%时就要告警，周限额和月限额要到95%告警。警示用户使用该模型可能会遇到限额限制。但是不会禁止用户使用。然后遇到告警时，系统可以告知用户去选择在最近的暂停点切换模型或者等待限额刷新后继续任务
工作流程
3.1 初始化阶段
1. 选择工作目录或创建工作目录
2. 选择预设的工作流程或者自定义工作流程，并支持保存已有的工作流来进行复用，这就需要能给工作流起名字
3. 对工作目录进行初始化
4. 基于工作流程确定产物有哪些
3.2 设计阶段
1. 基于项目的复杂度使用简单、中等、企业级的工作流来做处理
2. 设计阶段要自动按照版本号来创建主文件夹，避免后续归档的问题，一个版本的内容一开始就在当前版本内，
3. 每一个产物可以和ai不断对话打磨直到合适，
4. 每个产物生成后，都可以通过一些检查技能打磨
5. 前面产物有变化，要把变化同步到后续产物中，这个过程要通过ai来同步
6. 在工作流进行过程中，可新建新工作流继续生成新项目的产物，并且不同项目可以并行生成，
7. 生成的文档有prd、架构文档、pre-commit配置，ux文档及设计稿、开发story和验收标准，以及提交前的质量检查
  1. 对于新建的项目，当生成架构文档后，要自动生成pre-commit配置，并且要做到安全等方面的合规
  2. ux文档里的内容要生成html格式的说明，方便用户更直观的识别ux文档里提到的设计toknen
  3. 原型要使用html等形式展示交互稿，而且要有易用性检查。
8. 前后端开发项目要有openapi文档、错误状态码及错误处理统一机制
  1. 我希望openapi生成环节要有一个生成经验库，以后生成openapi文档时能参考经验库里的内容做设计，避免以前踩过的坑继续踩。这个经验库每个新项目在安装bmad时会自动携带。
  2. openapi接口文档生成后需要有一个要求用户确认的过程。确认过程能不能用类似swagger文档等类似可视化友好的内容来让用户确认。
9. 生成的文档要校验一下是否互相之间是表述一致的。
  1. prd和架构是否一致
  2. prd和ux文档是否一致
  3. ux文档和设计稿是否一致
  4. prd和架构、ux文档、设计稿、开发story和openapi是否一致
10. 还要生成编码规范文档，用来存放一些不是架构方面的内容，但又是编码方面有实现的内容
11. story生成时，如何能把开发任务和验证方法都能明确，验证方法尽可能做到能落地，能自动化验证，并且要说明要做到自动化测试需要什么工具，然后如何准备这些工具让ai能做自动化测试，不能自动化测试的部分是什么，并给出核心卡点是什么，我们可以通过开发什么工具来补齐这部分短板好能进行自动化测试。然后形成一个报告供用户去确认。
12. story生成越细越好，细致后完成一系列任务就做一个提交，或者完成一个任务就做一个任务的提交
13. 对于核心卡点的分析应该归档到子model指定目录里，然后对于批准实施的部分要去分精力做开发。然后并做验证。
14. 文档应该使用html形式来输出，或者使用易于阅读的格式输出
15. 前端编码前或者编码中要考虑有哪些地方可以封装为组件复用
16. 后端编码前或者编码中要考虑有哪些逻辑可以封装为公共方法复用
3.3 编码阶段
1. 生成好的设计文档，我可以拖拽进入到编码流程管道中去顺序执行产出，
2. 在管道里的任务可以在到达新阶段后设置暂停，来降低优先级，
3. 放入管道里处于非进行中状态的编码任务可以随时调整优先级。
4. 开发代码的环境应该在一个docker file配置好的虚拟化环境，防止删除宿主机上的文件，但又希望虚拟化环境里的文件变更和宿主机里指定文件是同步的
5. 开发计划里，完成一步就要提交完成的代码，然后第二步提交第二步的代码，修复bug提交修复bug 的代码。
3.4 验证阶段
1. 开发完成自动进行一系列自动化测试，要能做编译测试，单元测试，e2e测试，提交测试等一系列测试
2. 测试代码也有自己的最佳实践，也需要检查之类的。
3. 接口测试这里要有专门的机制能做到接口测试，并且遇到报错的情况要能修复
4. 前端也要做端到端测试
  1. 所有可以操作的部分都要有测试用例覆盖到，并且每一个可操作地方都要明确正确情况是啥，
5. 自动化验证阶段
6. Code review阶段需要根据使用了什么技术去静态化的缓存一些技术的文档或者最新写法之类，然后使用这些写法去做code review防止代码里写不符合技术自身写法的代码。然后这个阶段不能改代码，只能读代码。code review也有最佳实践比如architect / data structure / public interface / security / userability / etc.
7. bmad-accept里的验收内容是否能尽可能自动化，然后验收不合格的部分我希望能驱动bmad-Pipeline重新按照流程来修复并自我验证。
8. 质量门禁
9. 对抗性测试及每一次测试后找出来的问题归档，方便以后复用，形成一个清单，
10. bug修复
  1. ai分析bug时的输出应该用具体的格式框定
    1. 什么文件
    2. 什么方法
    3. 什么操作链路
    4. bug原因是什么
    5. 修复方案是什么，为什么有效
  2. 让ai分析根本原因，然后分析如何从机制上杜绝，然后分析修复计划，但不做任何修改。等人来做最后决策。
  3. 把自己发现的问题沉淀为可以自动化执行的测试用例
  4. 应该专门有一个专门的bug修复工作流，不管自动化验证还是人工验证阶段，直接启动这个工作流来修复bug。并归档形成专门的经验库和待办事项。
  5. 人工审核阶段，人工测试过程中，提出问题后，工作流会自动调用code-review-graph工具去生成代码图谱，然后根据代码图谱分析这个bug的基础原因，也就是到底是什么导致了问题，工作流程上的根本原因，如何从工作机制上避免这个问题出现。然后把这个bug的分析文档统一沉淀在指定的git仓库，
  6. 我建议文档按照bug类型先分类，然后bug分析文件标题带时间戳和版本。这样我直接拉取这些文档就可以根据总结机制更新工作流了。
  7. 如果能更加自动化，安装bmad工作流时，自动把这个仓库作为子model引入项目。
  8. 遇到bug，调用工作流分析bug原因，并且归纳出自动化验证机制，然后根据改动情况看是直接使用/plan修改还是创建story来修改。修改完后进行自动化验证。
  9. 验证通过后，把bug文档归档到子model指定目录里，然后根据工作流机制改变一栏的建议开始通过和用户交互完成工作机制改动计划，并改动工作流，改动完后自动提交到仓库里，并且能自动分发到指定的本地项目中。
11. 对于一些新增的内容或者调整，以及一些bug修复，要考虑是否增加爆炸半径计算，把被修改影响的地方都考虑到修改和验证修改的范围内，避免修改一处，然后其他地方出问题。
3.5 总结阶段
1. 对话总结
  1. 保存每一条和ai沟通过程中的对话，并且能够根据这些对话和对话得到的产物去分析和复盘。
  2. 要有复盘的标准
2. 代码提交
3. 经验总结与反思
  1. 我希望openapi生成环节要有一个生成经验库，以后生成openapi文档时能参考经验库里的内容做设计，避免以前踩过的坑继续踩。
  2. 生成架构文档、ux设计稿时，我也希望能形成对应的经验库。架构文档里可以存储各种软件的常见架构案例，以及架构生成方案，ux设计稿经验库里可以存储各种指定网站的设计token或者设计风格指南。
  3. 生成指定环节的文档时，能从git上查看已经形成的经验库里有啥经验。

1. 总体规划
  1. 工作台设置
  2. 归纳描述
    1. 我可以设置可编排的工作流，我可以同时拥有多个工作流，不同项目我可以用不同的工作流，不同的模型来完成，模型只和工作单元有关。
    2. 工作流由工作单元组成，工作单元可复用，工作单元可以增删改查，工作单元可以不断增加步骤或者设置来更完善
    3. 编码完成后，我希望你在做测试时，可以在一个指定容器里做测试，然后可以使用Chrome DevTools 协议创建用于处理 DOM的快照、屏幕截图等一切方便你排查问题的内容来辅助你完成自动化测试，并且看如何获取前端后端日志，前端后端性能指标，甚至前后端调用链路来辅助你完成自测和问题修复
    4. 测试通过后，希望学习hermes agent那样做事后回顾，并沉淀到公共知识库中
2. 初始化阶段
  1. 目标：在指定位置验证情况并补全目录结构
  2. 输出：在指定位置创建了文件夹，并补全了目录结构
3. 需求分析阶段
  1. 目标：明确用户要做什么、解决什么问题
  2. 输出：需求文档（PRD）
  3. 实现方法
    1. 明确需求文档包含什么内容，如何编写，检验准确性
4. 设计阶段
  1. 目标：
    1. 概要设计：系统架构、模块划分、技术选型
    2. 详细设计：接口设计、数据库设计、页面原型
  2. 输出：架构文档、目录划分文档、接口文档、数据库设计文档、页面设计图
  3. 实现方法
    1. 明确每个文档包含什么内容，如何编写，检验准确性
    2. 架构文档
    3. 数据库设计文档
    4. 页面设计图
    5. 目录划分文档
    6. 接口文档
5. 编码实现阶段
  1. 目标：完成开发
  2. 输出：前端代码、后端代码
  3. 实现方法：测试驱动开发、敏捷开发、code review、代码整洁、重构
6. 测试阶段
  1. 目标：校验代码是否符合设计
  2. 测试方法：单元测试、集成测试、接口测试、端到端测试、系统测试、回归测试，对抗测试、前端页面还原度测试
7. 提交到代码仓库
  1. 提交带版本的代码
8. 经验沉淀v
