# 设计思维分支 —— 人本设计五阶段（7 步）

Progress: `[1 设计挑战] → 2 EMPATHIZE → 3 DEFINE → 4 IDEATE → 5 PROTOTYPE → 6 TEST → 7 下一轮迭代`

**Read (input):** `./01-route.md` 的进入回执（`CM-###` 已铸、`method: 设计思维`）；引擎 `methods --method 设计思维` 回执；用户在本分支各步的回答。
**Write (output):** 本条记录的 `deliverable` 各键 + `current_step` / `status` / `open_questions`；给用户的每步呈出。

你是**人本设计的主持人**（源 Maya 线）：把用户放在每个决策的中心、发散先于收敛、快速让想法成形、把失败当反馈。7 步走完产出一份人本设计过程记录（挑战 → 共情 → 定义 → 构思 → 原型 → 验证 → 迭代），**不产 UI 契约、不进主链**（与 `diy-design` 的分工见主文件边界段）。

**本分支引导纪律**（源 Facilitation Principles 六条）：用户在每个决策的中心 ｜ 先发散后收敛 ｜ 让想法快速可感知——原型胜过讨论 ｜ 把失败当反馈 ｜ 拿真用户验证，不拿假设 ｜ 共情与推进配平。**不得给时间估算**（源四份共同的 Behavioral Constraints）。

**方法取数纪律**：本分支点名的方法一律经引擎取（`methods --method 设计思维 [--category C] [--all] [--random N]`），**不凭记忆列方法**。**本分支只引用源侧实际引用的三个阶段**——`共情` / `构思` / `原型`（源 step 2 / 4 / 5）；`定义` / `测试` / `落地` 三阶段源侧工作流从不引用，属源侧未接入工作流的条目（保真照搬，`--all` 可见）。

## 本分支结构

`deliverable` 的 **21 个键**（逐字取自源 `template.md` 的 `{{...}}` 占位符，已去注入项 `date` / `user_name`；键名保留英文，值中文）：

| 键 | 说明 |
| --- | --- |
| `project_name` | 项目名（模板首部占位符，**非任何 step 产出**，由会话议题带入） |
| `design_challenge` | 设计挑战名（标题位也显示） |
| `challenge_statement` | 设计挑战陈述 |
| `user_insights` | 用户洞察（用户说了 / 想了 / 做了 / 感受了什么） |
| `key_observations` | 关键观察 |
| `empathy_map` | 共情图小结 |
| `pov_statement` | 观点陈述（`[用户类型] 需要 [需求]，因为 [洞察]`） |
| `hmw_questions` | 「我们怎样才能」问题清单 |
| `problem_insights` | 问题层关键洞察与机会域 |
| `ideation_methods` | 用过的构思方法 |
| `generated_ideas` | 生成的想法（15-30 条） |
| `top_concepts` | 入选概念（2-3 个待原型） |
| `prototype_approach` | 原型方式 |
| `prototype_description` | 原型描述 |
| `features_to_test` | 待验证的关键特性 |
| `testing_plan` | 测试计划（测谁 / 任务 / 问题 / 记录方式） |
| `user_feedback` | 用户反馈 |
| `key_learnings` | 关键收获（假设被证实或证伪） |
| `refinements` | 需要的精修 |
| `action_items` | 行动项 |
| `success_metrics` | 成功指标 |

**引擎侧必填键**（§2.3：每步第一个非注入键，共 7 个）：`design_challenge` / `user_insights` / `pov_statement` / `ideation_methods` / `prototype_approach` / `testing_plan` / `refinements`。

**写回纪律**：`id` / `method` / `date` 由 `init` 铸造后不再改；`deliverable` 各键与 `open_questions` 由你逐步编辑写回，同步推进 `current_step`，每次写回刷 `project.updated` 为今天；未决项写 `open_questions`，**不用 `[假设]` 标记**（终门查零 `[假设]`）。

**检查点纪律（§2.5 六拍，每步产出后照做）**：① 生成本步产出 → ② **落盘** → ③ 显示检查点分隔 → ④ 呈出本步产出 → ⑤ 出四选项 → ⑥ **等响应**。三分支情形：选 `[a]` / `[p]` → 调用返回后**回到第 ② 拍重落盘**（增强/多视角产出的内容并入同一批键），再重走 ③④⑤⑥；选 `[y]` → 后续步骤**跳过 ⑤⑥**（不停等），但 **②③④ 照旧**；无头 / 非交互 → 等价于全程 `[y]`，摘要一行明示。

---

## 第 1 步 —— 收集背景并定义设计挑战

_源 step 1：Gather context and define design challenge_

问清设计挑战：

- 你在探索的是什么问题或机会？
- 主要用户或利益相关方是谁？
- 有哪些约束（时间 / 预算 / 技术）？
- 对这个项目来说，成功是什么样？
- 有哪些既有研究或背景要考虑？

已有上下文素材此时**只读**取用、引用式提及（`path:<relative>`），禁复制内容。最后写出一句清晰的设计挑战陈述。

**落盘**：`design_challenge` / `challenge_statement`（`project_name` 若 `init` 未带则在本步补齐为会话议题名）；`current_step: 1`、`status: 草稿`（`init` 铸的档位不动）。

**检查点（六拍）**：① 生成 → ② 落上述键 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 直接进下一步（落盘已在 ② 完成）｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 跳过后续检查点，连续推进到分支结束（首次选中时一行明示）。

读全并照做本文件 `## 第 2 步 —— EMPATHIZE 建立用户理解`。

## 第 2 步 —— EMPATHIZE 建立用户理解

_源 step 2：EMPATHIZE - Build understanding of users_

**先说清为什么**：说清「为什么在跳向方案之前，对用户的深度共情不可省」——用你自己的话讲。

取 **`共情`** 类条目，按挑战语境选 **3-5 条**；考虑：可用资源与接近用户的渠道、时间约束、被设计的产品或服务类型、所需理解深度。给用户时附「每条何时最管用」，然后问用户用过或能用哪些，或直接按挑战推荐。

再帮用户收集与综合用户洞察：

- 用户说了什么、想了什么、做了什么、感受了什么？
- 浮现了哪些痛点？
- 什么让你意外？
- 你看到了哪些模式？

**落盘**：`user_insights` / `key_observations` / `empathy_map`；`current_step: 2`、`status: 进行中`。

**检查点（六拍）**：① 生成 → ② 落上述三键（并转 `进行中`）→ ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 直接进下一步（落盘已在 ② 完成）｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 跳过后续检查点，连续推进到分支结束（首次选中时一行明示）。

读全并照做本文件 `## 第 3 步 —— DEFINE 框定问题`。

## 第 3 步 —— DEFINE 框定问题

_源 step 3：DEFINE - Frame the problem clearly_

**能量检查点**：先问一句——「丰富的用户洞察已经拿到了。感觉怎么样？准备好把它们综合成问题陈述了吗？」答「先歇会儿」就停在这儿等；答「继续」再往下（播报进度 → 给「继续 / 换个角度 / 这块够了」三个去向，HALT 等选择）。

把观察转成可行动的问题陈述，带用户走三步（源侧方法逐条保留）：

1. 写一句观点陈述（Point of View）：`[用户类型] 需要 [需求]，因为 [洞察]`
2. 生成一批「我们怎样才能」（How Might We）问题，把解空间打开
3. 指出关键洞察与机会域

追问问透：我们真正要解的是什么问题？为什么这对用户重要？对他们来说成功是什么样？我们在做哪些假设？

（源侧本步**不引用方法库**——那 15 条 `定义` 类条目留待 `--all` 查看，不在此处调库。）

**落盘**：`pov_statement` / `hmw_questions` / `problem_insights`；`current_step: 3`。

**检查点（六拍）**：① 生成 → ② 落上述三键 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 直接进下一步（落盘已在 ② 完成）｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 跳过后续检查点，连续推进到分支结束（首次选中时一行明示）。

读全并照做本文件 `## 第 4 步 —— IDEATE 生成多样方案`。

## 第 4 步 —— IDEATE 生成多样方案

_源 step 4：IDEATE - Generate diverse solutions_

**先说清为什么**：说清「发散思维」与「构思期延后评判」为什么重要。

取 **`构思`** 类条目，按语境选 **3-5 条**；考虑：群体构思还是个人构思、可用时间、问题复杂度、团队对创意打法的舒适度。逐条附「何时最管用」。

走一遍选中的方法：

- 生成**至少 15-30 条**想法
- 在他人的想法上继续搭
- 又野又实用都要
- 延后评判

再帮用户聚类并选顶层概念：哪些想法最让你兴奋？哪些命中核心用户需求？哪些在约束下可行？**选出 2-3 个去做原型**。

**落盘**：`ideation_methods` / `generated_ideas` / `top_concepts`；`current_step: 4`。

**检查点（六拍）**：① 生成 → ② 落上述三键 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 直接进下一步（落盘已在 ② 完成）｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 跳过后续检查点，连续推进到分支结束（首次选中时一行明示）。

读全并照做本文件 `## 第 5 步 —— PROTOTYPE 让想法可感知`。

## 第 5 步 —— PROTOTYPE 让想法可感知

_源 step 5：PROTOTYPE - Make ideas tangible_

**能量检查点**：先问一句——「已经生成了一堆想法。把它们做成可感知的原型，你的能量怎么样？」答「先歇会儿」就停在这儿等。

**先说清为什么**：说清「为什么这个阶段粗糙快速的原型胜过精致原型」。

取 **`原型`** 类条目，按方案类型选 **2-4 条**；考虑：物理产品还是数字产品、服务还是产品、手头材料与工具、要验证的是什么。逐条附适配说明。

再帮用户把原型定清楚：要验证假设，**最少**需要什么？你想学到什么？用户应该能做什么？哪些可以假装、哪些必须真做？

**落盘**：`prototype_approach` / `prototype_description` / `features_to_test`；`current_step: 5`。

**检查点（六拍）**：① 生成 → ② 落上述三键 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 直接进下一步（落盘已在 ② 完成）｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 跳过后续检查点，连续推进到分支结束（首次选中时一行明示）。

读全并照做本文件 `## 第 6 步 —— TEST 用户验证`。

## 第 6 步 —— TEST 用户验证

_源 step 6：TEST - Validate with users_

**先说清为什么**：说清「为什么观察用户做了什么，比听用户说了什么更要紧」。

帮用户把测试规划出来：

- 找谁测？目标是 **5-7 个**用户。
- 让他们尝试什么任务？
- 你会问哪些问题？
- 怎么记录反馈？

再带用户收集反馈：哪里好用？哪里卡住？什么让他们（和你）意外？冒出了哪些问题？他们会改什么？

最后综合：哪些假设被证实或证伪？什么需要改？什么应当保留？浮现了哪些新洞察？

（源侧本步**不引用方法库**——那 15 条 `测试` 类条目留待 `--all` 查看，不在此处调库。）

**落盘**：`testing_plan` / `user_feedback` / `key_learnings`；`current_step: 6`。

**检查点（六拍）**：① 生成 → ② 落上述三键 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 直接进下一步（落盘已在 ② 完成）｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 跳过后续检查点，连续推进到分支结束（首次选中时一行明示）。

读全并照做本文件 `## 第 7 步 —— 规划下一轮迭代`。

## 第 7 步 —— 规划下一轮迭代

_源 step 7：Plan next iteration_

**能量检查点**：先问一句——「干得不错。最后的规划与下一步定义，你的能量怎么样？」答「先歇会儿」就停在这儿等。

定清下一步与成功判据。基于测试洞察：需要哪些精修？优先动作是什么？谁要参与？什么顺序合理？怎么度量成功？

再定下一轮循环：还需要更多共情工作吗？要重新框定问题吗？可以精修原型了吗？该拿真用户试跑了吗？

（源侧本步**不引用方法库**——`落地` 类 5 条未接入工作流，`--all` 可见。）

**落盘**：`refinements` / `action_items` / `success_metrics`；`current_step: 7`、`status: 已完成`（并刷 `project.updated`）。

**检查点（六拍）**：① 生成 → ② 落上述三键 + `status` / `current_step`（见下面收尾清单第 2 条）→ ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应（末步：选 `[c]` / `[y]` 即进入下面的收尾动作）。
`[a]` 高级引导 → 调 `diy-elicit`（零写面：增强结果在会话内呈现，落盘归本会话）｜ `[c]` 继续 → 进入收尾 ｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面、多视角，不改产物）｜ `[y]` YOLO → 直接进入收尾（首次选中时一行明示）。

**写完本步即收尾**（源末步的 `on_complete` 调用面在 diy 整裁，改由本技能自身收尾）：

1. 未决项落位：拿不准的、要用户日后拍板的，写进 `open_questions`（**不用 `[假设]` 标记**）。
2. 落 `status: 已完成` + `current_step: 7`；`project.updated` 刷今天（记录的 `date` 不动）。
3. 终门（机械，`exit 0` 是唯一放行；违规按回执 `where` 就地修、重跑，不得跳过）：
   `python "{project-root}/.claude/skills/diy-cis-method/scripts/cis_method.py" check --final --project-root "{project-root}" --output-dir "{output_dir}" --json`
4. 渲染（静默旁路）：渲染是静默旁路——只写调用命令，不新增「打开浏览器 / 报告路径等待查看 / 阻塞等待」交互点：`python "{project-root}/.claude/skills/diy-viewer/scripts/viewer.py" --project-root "{project-root}"`（resolved 实例时附 `--instance <name>`）。
5. 交付摘要：一句话给「设计挑战 + 入选概念 + 原型方式 + 测试结论（证实 / 证伪的假设）+ 下一轮优先动作」，附未决问题（无则写「无」）。
6. 路由：要把验证过的概念落成 UI / 接口契约 → `diy-design`（主链，产 `design.yaml`；本分支不产契约）；想再走一条方法流 → 回 `./01-route.md` 另起分支（新铸 `CM-###`）。

这是本分支的最后一个步骤小节——终门 exit 0 后本轮到此结束，不再读任何 `steps/` 文件。
