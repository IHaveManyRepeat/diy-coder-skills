# Step 7 — 平台段、收尾与 74 项校验（Finish）

Progress: `[1 平台启动] → 2 技术栈 → 3 集成 → 4 联系策略 → 5 多语言 → 6 平台定稿 → 7 分析 → 8 摘要 → 9 日志与激活 → 10 74 项质量校验`

**Read (input):** `./06-visual.md` 的放行（`intake.stage: 收尾`）；`brief.core` 的 `platform_strategy` 与 `constraints`（本段平台决策要跟它们一致）；`brief.content` 的 `languages`（多语节要对齐）；用户在本文件各步的回答。
**Write (output):** `brief.platform.platform_requirements` 四个键；`project.status: 已定稿` + `intake.stage: 收尾`；`revisions`；给用户的交付摘要与路由。

你是**收尾的主持人**（源 wds-1 的平台链 + 33–36 步）。前六节补完简报的最后一段，第 7–9 节做分析与收尾，**第 10 节是 74 项机械质检**——它是本技能的终门前置。

## 第 1 步 —— 平台段启动（源 step-27）

**先说清为什么**：讲清「为什么平台需求要写进简报」——它决定后面所有设计决策能不能落地（源侧口径：技术约束是设计参数）。用你自己的话讲。

**开场三问**：这一段的产出是什么（技术栈 / 集成 / 联系 / 多语四项）？前面有没有已经聊过的技术倾向（核心段的 `platform_strategy`）？**上线后谁维护**？

**落盘**：`brief.platform.platform_requirements` 建空骨架；与核心段 `platform_strategy` 对一次表。

**检查点（六拍）**：① 生成 → ② 落骨架 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面）｜ `[c]` 继续 → 进下一步 ｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面）｜ `[y]` YOLO → 跳过后续检查点连续推进（首次选中时一行明示）。

读全并照做本文件 `## 第 2 步 —— 技术栈`。

## 第 2 步 —— 技术栈（源 step-28）

**问**：**CMS / 框架**用什么（或「让承接方定」）？**前端与样式**路线？**托管与部署**在哪？**哪些技术约束是硬的**（客户 IT 政策、既有系统、合规）？

**口径**：本技能**不替用户选型**——用户没主意就明确落「待承接方定 + 已知约束清单」，不写拍脑袋的技术名。〔简化档：**本节必走**——是简化档在平台段的唯一必填键。〕

**落盘**：`brief.platform.platform_requirements.tech_stack`。

**检查点（六拍）**：① 生成 → ② 落 `tech_stack` → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 3 步 —— 集成`。

## 第 3 步 —— 集成（源 step-29）

**问**：要接**哪些第三方**（表单、支付、CRM、分析、客服、地图…）？**每项为什么要它**（源侧质检硬要求：每项必须写用途）？**哪些是现在就要、哪些是以后再说**？**彼此有没有冲突**（两套分析工具、两个客服台）？

**落盘**：`brief.platform.platform_requirements.integrations`（列表；每项含名称 + 用途 + 现在/将来）。

〔简化档：本节标〔可跳过〕。〕

**检查点（六拍）**：① 生成 → ② 落 `integrations` → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 4 步 —— 联系策略`。

## 第 4 步 —— 联系策略（源 step-30）

**问**：用户**怎么找你**（表单 / 邮件 / 电话 / 聊天 / 微信）？**主渠道**是哪一个？**表单要收哪些字段**（字段越多转化越低——要跟用户确认取舍）？有没有**服务时段**要说明？

**口径**：本节直接影响 UX 设计（源侧口径：联系方式的复杂度会决定页面的交互设计）。

**落盘**：`brief.platform.platform_requirements.contact_strategy`。

〔简化档：本节标〔可跳过〕。〕

**检查点（六拍）**：① 生成 → ② 落 `contact_strategy` → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 5 步 —— 多语言`。

## 第 5 步 —— 多语言（源 step-31）

**问**：**支持哪些语言**（跟内容段的 `languages` 对表，冲突以内容段为准）？**语言切换**放哪、怎么切？**多语的 URL 结构**怎么定（子目录 `/en/` / 子域 / 参数）？**翻译流程**走哪条？

**单语时**：明确落「单语言，无多语需求」——**空着不算回答**。

**落盘**：`brief.platform.platform_requirements.multilingual`。

〔简化档：本节标〔可跳过〕。〕

**检查点（六拍）**：① 生成 → ② 落 `multilingual` → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 6 步 —— 平台文档定稿`。

## 第 6 步 —— 平台文档定稿（源 step-32）

1. **回述四键**：技术栈 / 集成 / 联系 / 多语念一遍给用户确认。
2. **维护归属**：明确写下**上线后谁维护**（源侧这一步的硬要求）；技术栈里若含客户看不懂的选型，当场解释一句。
3. **不落 md**：源侧这一步定稿 `platform-requirements.md` 全文——**diy 侧不落盘 md**，成品是 `brief.platform.platform_requirements` 四键。（源侧另有一份从未被引用的 `platform-requirements.template.yaml` 孤本，字段划分可参照但不照抄——目录名与版本号都是源的。）
4. **推进**：四段齐备。

**检查点（六拍）**：① 生成 → ② 落定稿确认 → ③ 分隔 → ④ 呈出平台段成稿 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 7 步 —— 简报分析`。

## 第 7 步 —— 简报分析（源 step-33）

**源侧这一步不产文件**（静默分析）——diy 侧同样**不落盘**，只在会话内做，为下一步交接包备料。

**做法**：把四段过一遍，抽出交接给下游 `diy-wds-trigger` 的四组键（§2.3.1 冻结契约）：

- `brief.core` 的 **vision / positioning / target_users / product_concept**（下游据此起业务目标与人物群）
- `brief.content` 的 **content_language**（下游据此定语气口径）
- `brief.visual` 的 **visual_direction** 六维（下游的场景段与设计段要用）
- `brief.platform` 的 **platform_requirements**（下游据此刻平台约束）

**自检**：上列四组键**是否都非空**？空的那个当场回去补——下游按 `project.status: 已定稿` 门禁读，读不到就整条链停。

## 第 8 步 —— 交接摘要（源 step-34）

**源侧这一步同样不产文件**——产出是**给用户看的一页交接包**（会话内呈出，不落盘）。

写四块：① **一句话项目**（愿景 + 定位）；② **四段要点**（核心 / 内容 / 视觉 / 平台各 2–3 行）；③ **下游要拿它做什么**（触发图 / 场景 / 设计三段怎么接）；④ **已知待决**（写进 `revisions` 的条目，一条一句）。

**落盘**：`revisions` 追加待决条目（`change` 点名节名，`reason` 写为什么还没定）。

## 第 9 步 —— 设计日志与激活（源 step-35 + step-36）

〔**源侧缺陷修复**〕源 step-35 要求把进度写进 `00-design-log.md` 的 `## Progress` 与 `## Key Decisions` 两节，而**该日志模板里根本没有这两节**（census-1 §5-⑤ 第 2 条）；step-36 又要激活两个**不存在的路径**（`workflows/wds-2-trigger-mapping/instructions.md` 与 `agent-activation/wds-saga-analyst.md`）。diy 侧按单一源纪律收口：**进度 = `intake.stage` + `project.updated`；决策 = `revisions`；下游激活 = 本技能自己的收尾路由**，不建散 md、不指幽灵路径。

1. **定稿落盘**：`project.status: 已定稿` + `intake.stage: 收尾` + `project.updated` 刷今天。
2. **终门（机械，`exit 0` 是唯一放行；违规按回执 `where` 就地修、重跑，不得跳过）**：
   ```
   python "{project-root}/.claude/skills/diy-wds-brief/scripts/wds_brief.py" check --final --project-root "{project-root}" --output-dir "{output_dir}" --json
   ```
3. **渲染（静默旁路）**：`python "{project-root}/.claude/skills/diy-viewer/scripts/viewer.py" --project-root "{project-root}"`（resolved 实例时附 `--instance <name>`）。
4. **激活下游**：一句话给「本简报已定稿，可进入触发图（`diy-wds-trigger`）或场景（`diy-wds-scenarios`）」——**WDS 线是自己的短链**，diy 主线的 `diy-design` / `diy-dev` 在 C·3 之前不接这条线。
5. **交付摘要**：一句话给「项目名 + 愿景 + 定位 + 平台策略 + 视觉主风格 + 待决项」，附未决问题（无则写「无」）。

## 第 10 步 —— 74 项质量校验（源 steps-v 六步）

**源侧缺陷修复（两处，先读）**：
① 源校验链独立成 `workflow-validate.md` + `steps-v/` 六步、产一份 `validation-report.md`——**diy 侧裁掉这条独立子流程与报告产物**（裁定 15：终门 = `check --final`，复检 = 重跑 `check`；另立 md 报告 = 双实现），**校验维度与阈值全部保留在本节**。
② 源的第二步校验 `B-Trigger-Map/`——那是 **`diy-wds-trigger` 的产物**，简报阶段根本不存在（census-1 §5-⑤ 第 7 条的反向依赖缺陷）。**本批的修法**：下表的「触发图一致」组**只核简报侧的供给**（`brief.core` 的目标 / 用户两键在场，够下游起图即可）；**触发图自身的链式一致性由 `diy-wds-trigger` 的 `06-finish.md` 核**——本组标〔下游复检〕，不在本技能判红。
③ 源侧第三至六步读的是 `.md` 产物——**diy 侧一律改读本产物的对应键**（下表 where 已写明）。

**逐项核对（74 项，机械过一遍；不合格的回对应步骤文件修，修完重跑本节的项）**：

**一、简报完备（源 steps-v 第 1 步，15 项）**
- [ ] `brief.core.vision` 清晰具体，不是套话
- [ ] `brief.core.positioning` 含目标 / 需求 / 品类 / 好处 / 差异点
- [ ] `brief.core.business_model` 已定义
- [ ] `brief.core.target_users` 至少一份带行为描述的画像
- [ ] `brief.core.success_metrics` 至少 3 条可量指标
- [ ] `brief.core.competitive_landscape` 至少 2 个替代方案已分析
- [ ] `brief.core.constraints` 已记录（哪怕写「无已识别约束」）
- [ ] `brief.core.platform_strategy` 已陈述
- [ ] 〔简化档〕项目摘要在场（`vision`）
- [ ] 〔简化档〕目标受众已识别（`target_users`）
- [ ] 〔简化档〕关键目标已陈述
- [ ] 〔简化档〕平台已注明（`tech_stack`）
- [ ] 每节都有实质内容，不是只有标题
- [ ] 零 TODO / 占位标记残留
- [ ] 每节内容与其节名相符

**二、触发图一致（源 steps-v 第 2 步，14 项；本组对简报侧只核供给，〔下游复检〕项随 `diy-wds-trigger`）**
- [ ] 〔下游复检〕触发图的 `businessGoal` 具体可量
- [ ] 〔下游复检〕触发图的 `solution` 描述我们怎么帮用户
- [ ] 〔下游复检〕触发图的 `user` 指明我们在帮谁
- [ ] 〔下游复检〕`drivingForces.positive` 至少 2 条
- [ ] 〔下游复检〕`drivingForces.negative` 至少 2 条
- [ ] 〔下游复检〕`customerAwareness.start` 已定义
- [ ] 〔下游复检〕`customerAwareness.end` 已定义
- [ ] 〔简报侧供给〕`brief.core` 有可作为业务目标的陈述（供下游起图）
- [ ] 〔简报侧供给〕`brief.core.target_users` 可作为触发图的 user 来源
- [ ] 〔下游复检〕驱动因素具体（不是「希望它能用」这类空话）
- [ ] 〔下游复检〕认知旅程逻辑成立（起点 ≠ 终点）
- [ ] 〔下游复检〕多张触发图之间无相互矛盾的业务目标
- [ ] 〔下游复检〕多张触发图的用户互异或代表不同语境
- [ ] 〔下游复检〕多张触发图的驱动因素无无理由重复

**三、SEO 策略（源 steps-v 第 3 步，10 项）**
- [ ] 主关键词至少 3 条
- [ ] 每条关键词标了搜索意图（信息 / 导航 / 交易）
- [ ] 关键词与 `brief.core` 的业务目标相关
- [ ] 已补长尾变体（适用时）
- [ ] 每条主关键词至少挂到一页
- [ ] 两页不抢同一个主关键词
- [ ] 页-词分配与项目范围现实相符
- [ ] 关键词表格式可被下游场景段引用
- [ ] SEO 优先级与用户优先级一致
- [ ] 内容结构支持关键词落位

**四、内容与语言（源 steps-v 第 4 步，12 项）**
- [ ] 人格词至少 3 个
- [ ] 人格词具体（不是「专业」「友好」这类谁都能贴的词）
- [ ] 人格与 `brief.core.target_users` 的期待一致
- [ ] 语气属性配了「我们会说 / 绝不说」对照例
- [ ] 语气与人格一致
- [ ] 语气分场景可调（报错 vs 营销）
- [ ] 主语言已指定
- [ ] 附加语言已列出（多语时）
- [ ] 正式度已定义（你 / 您、tu / vous）
- [ ] 写作风格指南在场
- [ ] 内容结构范式已记录（适用时）
- [ ] 与 SEO 关键词策略对齐（有 SEO 时）

**五、视觉方向（源 steps-v 第 5 步，12 项）**
- [ ] 既有品牌资产已记录（或明确落「无既有品牌」）
- [ ] logo 使用规范（适用时）
- [ ] 色板已定义或已引用
- [ ] 至少 2 个参考站已记录
- [ ] 每个参考站写明取什么（不是「我喜欢这个站」）
- [ ] 参考站与项目类型相关
- [ ] 设计风格已描述
- [ ] 布局偏好已记录
- [ ] 特效偏好已记录（动画、过渡）
- [ ] 摄影风格已定义（用照片时）
- [ ] 插画风格已定义（用插画时）
- [ ] 图片来源策略已注明

**六、平台需求（源 steps-v 第 6 步，11 项）**
- [ ] CMS / 框架已指定（或明确落「待承接方定 + 约束清单」）
- [ ] 托管方式已注明
- [ ] 技术约束已记录
- [ ] 第三方集成已列出
- [ ] 每项集成写明用途
- [ ] 集成选型之间无冲突
- [ ] 联系表单要求已记录
- [ ] 沟通渠道已指定（邮件 / 聊天 / 电话）
- [ ] 语言切换方式已定义（多语时）
- [ ] 多语的 URL 结构已指定
- [ ] 翻译流程已注明

**74 项过完** → 回第 9 步第 2 条跑终门 `check --final`。**本文件到此结束**——终门 exit 0 后本轮收尾，不再读任何 `steps/` 文件。
