# Step 9 — 全活动质检、提示词导出与定稿（Finish）

Progress: `[1 全活动质检] → [2 提示词导出包] → [3 迭代纪律：3 轮精修与 4 条红旗] → [4 定稿与终门]`

**Read (input):** `{output_dir}/wds-assets.yaml` 全部记录（`activities[]` / `prompts[]` / `presentation[]`）；`{output_dir}/wds-scenarios.yaml` 的 `scenarios[].pages[]`（**覆盖率对表**：哪些页一件资产都没有）；`data/iteration-refinement.md`；`data/stop-red-flags.md`；`templates/prompt-export.template.md`；`check` 回执。
**Write (output):** 质检结论与修复；`prompts[]` 的 `exported` 翻转与导出文件；`project.status: 已定稿`；`project.updated` 刷今天；`revisions`；给用户的交付摘要与路由。

你是**收尾的主持人**（各活动源 workflow 的 AFTER COMPLETION + 源 `workflow.md:98–103` 的提示词导出兜底）。前三步是本技能的质量门槛与**唯一生成通道**，第四步才是定稿与终门。

**本段纪律**：① **不许橡皮图章**——逐条过，不合格回对应步骤文件修；② **提示词导出是本技能唯一的生成路径**（裁定 6：不接任何外部服务），第 2 步不能省；③ **终门唯一放行 = `check --final` 的 `exit 0`**。

## 第 1 步 —— 全活动质检（跨活动一致 + 覆盖率）

**先说清为什么**：八个活动各自评过自己，但**没人评过它们放在一起**——这一步评的是活动的**相互一致**与**覆盖完整**。用你自己的话讲。

**跨活动一致五查**（逐条过，不合格回对应活动修）：
- [ ] **同一页的资产互相认识**——页面稿用到的图标 / 图 / 文案，在对应活动里**都有条目**（页面稿别用不存在的图标）
- [ ] **尺寸同源**——线框 / 页面稿 / UI 件三处的同名构件尺寸一致（栅格不漂）
- [ ] **令牌同源**——凡有 `token_ref` 的条目引的是同一套令牌键（无设计系统降级时**全留空**，不许一半有一半没有）
- [ ] **风格不打架**——视觉活动的 `style.design` 指向同一张卡或**有理由不同**（不同要写在 `revisions` 里）；`AS-08` 的视觉语言独立（跨域，不参与本查）
- [ ] **文案与页面稿对得上**——页面稿上的真实文案来自 `AS-07` 的成稿，不是现编的

**覆盖率对表**（机械取 `scenarios[].pages[]` 与全部 `items[].pages[]` 对比，引擎在 `check` 回执里给 `coverage`）：
- `unassigned`——有页面但一件资产都没有的页（**要么补、要么在 `revisions` 写出为什么这页不需要**）；
- `orphan`——挂了不存在的页面 ID 的条目（引用错了，改）。

**活动级的收口**：未做的活动记 `status: 已跳过` + 一行理由（**不许留 `未开始`**）；做了的画 `已评审`。

**落盘**：质检结论与修复。

**检查点（六拍）**：① 生成质检表与覆盖率表 → ② 落盘修复 + 活动 `status` 收口 → ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。
`[a]` 高级引导 → 调 `diy-elicit`（零写面）｜ `[c]` 继续 → 进下一步 ｜ `[p]` 多方模式 → 调 `diy-party-mode`（零写面）｜ `[y]` YOLO → 跳过后续检查点连续推进（首次选中时一行明示）。

读全并照做本文件 `## 第 2 步 —— 提示词导出包`。

## 第 2 步 —— 提示词导出包（**本技能唯一的生成通道**）

**先说清**：本技能**不连任何外部服务**（裁定 6，用户拍板：「我直接出 html 就行了」）。**每条资产的生成 = 把提示词导出去，由用户在外生成后回填**——所以这一步不是收尾杂活，是**出口**。

**逐条导成可粘贴文件**：按 `templates/prompt-export.template.md` 的骨架（改造自源 `stitch-prompt.template.md`，**已去 Stitch 服务绑定与配额段**），把每条 `prompts[]` 条目落成一份 `.md`：
- **落点**：`{output_dir}/assets/<活动>/prompts/<条目名>.md`（与 `prompts[].file` 一致）；
- **内容**：资产名 / 服务目标 / 规格锚（页面 ID / 尺寸 / 令牌）→ 正文提示词整段 → 交付回填说明；
- **回填约定**：用户在外生成后的产物放回 **`{output_dir}/assets/<活动>/`**（子目录、不入库，裁定 12），并把路径写进对应条目的 `items[].assets[].path`——**YAML 只存路径引用，不嵌二进制**。

**翻转导出状态**：全部条目 `prompts[].exported` 从 `false` 翻 `true`（**已导出的才是可交付状态**）；回填完成的条目在 `items[].assets[]` 补路径。

**导出包清单**（会话内呈出一页，供用户带走）：条目 ID / 资产名 / 目标服务 / 文件路径 / 是否已回填——**四条都要有具体值**。

**登记（必写进回报）**：本步导出的文件落在**子目录**，而 `viewer.py` 只扫 `output_dir` 顶层 `*.yaml` → **`assets/<活动>/` 下的产物（含提示词包）渲染不到**；viewer 里能看到的只有 `wds-assets.yaml` 的结构与本步的导出清单。

**检查点（六拍）**：① 生成导出文件 → ② 落盘 `prompts[].exported` / 导出文件 → ③ 分隔 → ④ 呈出导出包清单 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 3 步 —— 迭代纪律：3 轮精修与 4 条红旗`。

## 第 3 步 —— 迭代纪律：3 轮精修与 4 条红旗（**裁定 6 的摘留，逐条保留**）

**先说清为什么**：本技能已把外部的 Figma / Stitch 通道全裁（裁定 6），但有**三条与外部服务无关的纪律**必须留下——它们描述的是「**怎么迭代**」与「**什么时候该停**」。逐条在会话里过。

### 3.1 三 轮 迭 代 精 修（源 `prototype-to-figma-workflow.md` 的迭代环，摘留全文见 `data/iteration-refinement.md`）

迭代三拍（**外部的 Figma 那一拍换成「用户在外生成 / 本地改 HTML」**，语义不变）：
1. **基本版**——先出能用但不精致的件（功能对、样子糙）。**评估**：功能对吗？精致吗？
2. **视觉精修**——补字阶 / 色板 / 间距 / 阴影 / 变体 / 态，**并把改出来的值记下来**（记的是**值**，不是截图）。
3. **用增强后的设计系统重渲**——把第 2 拍得到的新令牌值回填到设计系统（**归 `diy-wds-system`，本技能只提 `revisions` 建议**），再用新令牌重出。

**判据**：一轮结束时必须能回答「这一轮改了什么值、为什么」；答不出来就不是精修，是瞎调。

### 3.2 四 条 红 旗（源 `when-to-extract-decision-guide.md:517–535`，完整版见 `data/stop-red-flags.md`）

**任何一条亮起 → 停下来问用户，不继续做**：
- 🚩 **过早优化**——概念还没验证就在打磨（先验功能，后打磨）；
- 🚩 **过度工程**——为一个一次性页面建设计系统（一次性的页就保持简单）；
- 🚩 **分析瘫痪**——改个没完、永远不出货（**给「够好」定一条线**）；
- 🚩 **工具崇拜**——因为能用某件工具而用它，不是因为该用（**盯结果，不盯工具**）。

**落点**：这四条是**本活动与所有生成活动的停止闸**——出现任一条，把 `activities[].items[]` 停在当前状态并在 `revisions` 写明「因红灯 X 停止」，**不假装做完**。

**检查点（六拍）**：① 生成迭代记录与红灯巡检结论 → ② 落盘 `revisions`（有红灯才写）→ ③ 分隔 → ④ 呈出 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。

读全并照做本文件 `## 第 4 步 —— 定稿与终门`。

## 第 4 步 —— 定稿与终门

### 4.1 定稿落盘

`project.status: 已定稿` + `project.updated` 刷今天；`stage: 收尾`。**前置条件**（引擎会核）：全部活动 `status` ∈ {`已评审`, `已跳过`}，无一停在 `未开始` / `进行中`。

### 4.2 终门（机械；`exit 0` 是唯一放行）

```
python "{project-root}/.claude/skills/diy-wds-assets/scripts/wds_assets.py" check --final --project-root "{project-root}" --output-dir "{output_dir}" --json
```

`--final` 核：`project.status: 已定稿` + `stage: 收尾` + **8 个活动码齐备** + 全部条目必填键齐（`name` / `pages` / `prompt` / `assets` 路径 / `review.verdict`）+ 父子 ID 同源 + `assets[].path` **落在 `assets/<活动>/` 内** + 提示词条目有来源 + `presentation[].recipe` 七值之一 + **零 `[假设]`**。**按回执 `where` 就地修、重跑，不得跳过**；渲染与收尾都等 `exit 0`。

### 4.3 渲染（静默旁路）

`python "{project-root}/.claude/skills/diy-viewer/scripts/viewer.py" --project-root "{project-root}"`（resolved 实例时附 `--instance <name>`）。

### 4.4 交付摘要与路由

**交付摘要**（会话内，不落盘）：项目名 / 各活动条目数 / 已导出提示词数 / 已回填资产数 / 覆盖率 / 未做的活动与理由 / **红灯记录**（有则点名）。

**路由一句话**：本技能是 **WDS 线的链条终点**——源侧 `E-Assets` 的消费者为零（普查实测），diy 侧的回填口随 C·3（`diy-design` 的 WDS 模式）接线；本线此前四个产物（`wds-brief` / `wds-trigger` / `wds-scenarios` / `wds-design-system`）与本产物一起等下一位消费者。

**本文件到此结束**——不再读任何 `steps/` 文件。

**检查点（六拍）**：① 生成摘要 → ② 落盘 `project.status: 已定稿` / `stage: 收尾` / 终门通过 → ③ 分隔 → ④ 呈出摘要 → ⑤ 出四选项 → ⑥ 等响应。四选项同第 1 步。
